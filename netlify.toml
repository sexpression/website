[build]
    command = "npm run build"
    environment = {SENDGRID_API_KEY = "", HOOK_REBUILD_TOKEN = ""}
    functions = "functions/"
    publish = "dist/"

[[redirects]]
    from = "/api/*"
    status = 200
    to = "/.netlify/builders/:splat"

[functions."build"]
schedule = "@daily"